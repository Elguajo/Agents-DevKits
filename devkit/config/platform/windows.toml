# Windows-specific Codex settings belong here. It is intentionally empty until
# a Windows-native integration is verified. Machine paths belong in the ignored
# machines/<hostname>/codex.toml layer instead.
