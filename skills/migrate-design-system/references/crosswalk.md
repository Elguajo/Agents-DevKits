# Design-system crosswalk

For every migrated role, record the source token or component, target token or component, semantic intent, state/theme behavior, consumer impact, and migration status.

Use an alias or bridge layer when both systems must coexist. Mark mappings as exact, adapted, missing, or intentionally unsupported. A visually similar color is not an equivalent role if its contrast, state, or destructive-action meaning differs.
