# Figma sync contract

For each synchronization scope, record:

- source of truth and target;
- token namespaces or variable collections involved;
- modes (theme, brand, density) and their mapping;
- component variants and states checked;
- provider action actually performed, if any;
- drift, conflicts, or manual work remaining.

Never treat a planned sync as proof that Figma and code are in parity.
